// Serviço HTTP / configuração da API (colocado em lib para não ser tratado como rota)
// Ajuste o IP conforme seu ambiente (Expo Go em dispositivo físico precisa do IP da máquina)
export const API_URL = "http://192.168.2.44:3000"

// Exemplos: ver o ip no windows com `ipconfig` e no mac/linux com `ifconfig`
// - Android emulator: http://10.0.2.2:3000
// - iOS simulator: http://localhost:3000
// - Dispositivo físico: http://<SEU_IP_LOCAL>:3000
